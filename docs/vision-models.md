# Vision Model Probe Results

Test image: 320x200 PNG, solid blue background (#228BE6), white bold text `XENA-42`.
Generated by `scripts/gen-vision-test-image.ps1`; probed by `scripts/probe-vision-models.ps1`.

## Probe log (2026-08-25)

| Model | Result | Note |
|---|---|---|
| `tokenrouter/google/gemini-3-flash-preview` | FAIL 404 | no active credentials for provider |
| `bzl/gemini-3-flash-preview` | FAIL 404 | no active credentials for provider |
| `gc/gemini-2.5-flash` | FAIL 404 | "No active credentials for provider: gemini-cli" |
| `af/google/gemini-2.5-flash` | FAIL 404 | no active credentials for provider |
| `gemini/gemini-2.5-flash` | FAIL 404 | "No active credentials for provider: gemini" |
| `cu/gemini-3-flash-preview` | FAIL 404 | "No active credentials for provider: cursor" |
| **`oc/x-preview-f-free`** | **PASS** | correct answer, see below |

## PASS detail — `oc/x-preview-f-free`

- Request: text + `data:image/png;base64` image_url, `max_tokens=400`
- Response content: *"The background is blue, and the text says \"XENA-42\"."*
- Upstream resolution: router maps it to `mimo-v2.5-free`
- Caveat: it is a reasoning-style upstream — keep `max_tokens >= ~300` so reasoning
  doesn't starve the visible answer; ask for "no reasoning" to keep replies short.
- Cost field returned `"0"` (free tier).

## Verdict

Xena vision model = **`oc/x-preview-f-free`** (set in `.env` as `XENA_VISION_MODEL`).
Probing stopped at first reliable pass per free-tier etiquette.
